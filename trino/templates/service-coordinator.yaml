apiVersion: v1
kind: Service
metadata:
  name: trino-coordinator
  namespace: {{ .Release.Namespace }}
spec:
  type: NodePort
  selector:
    app: trino
    role: coordinator
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 31104
